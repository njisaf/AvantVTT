{{!--
Universal Item Header Partial - Stage 2 Refactor

This partial provides a simplified header layout containing ONLY icon + name.
All other fields (AP cost, level requirements, etc.) have been moved to the body
for the universal item sheet architecture.

Required context:
- item: The item data object

Features:
- Single row: icon + name only
- Consistent across all item types
- Meta fields moved to sheet-body for better organization
--}}

<header class="sheet-header">
    {{!-- Row 1: Icon + Name (ONLY content in header) --}}
    <div class="header-main row-container">
        {{!-- Image Upload Component --}}
        {{> "systems/avant/templates/shared/partials/image-upload.hbs"
        src=item.img
        alt=item.name
        title=item.name
        size=48
        class="item-icon"}}

        {{!-- Name Field --}}
        {{> "systems/avant/templates/shared/partials/text-field.hbs"
        name="name"
        value=item.name
        label="Item Name"
        placeholder=(concat "Enter " (titleCase item.type) " name...")
        required=true
        class="item-name-field"}}
    </div>
</header>
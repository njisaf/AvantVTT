/**
 * @fileoverview Item Sheet Layout Helpers
 * @description Helpers specific to item sheet form layouts
 * @version 1.0.0
 * @author Avant Development Team
 */

import { field, when, fullWidth } from '../shared/helpers';
import type { Field } from '../shared/types';

/**
 * Item sheet specific helpers
 *
 * Note: The commonFields object has been moved to ../shared/helpers.ts
 * for use across all layout types (item-sheet and item-card).
 *
 * This file is reserved for item-sheet specific helper functions
 * that are not needed by the card layout system.
 */

// Currently no item-sheet specific helpers are needed.
// All common field functions are now in ../shared/helpers.ts 